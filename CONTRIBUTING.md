# Contributing

Use small semantic commits, keep fake-token validation as the default, and run `dotnet build TelegramAutomation.slnx -c Release` plus `dotnet test TelegramAutomation.slnx -c Release` before opening a PR.
