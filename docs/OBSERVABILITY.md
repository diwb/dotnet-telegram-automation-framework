# Observability

The core exposes ActivitySource `TelegramAutomation` and Meter `TelegramAutomation`. Metrics include updates received/processed, handler duration/failures, duplicate updates, Telegram API calls, retries and rate-limited events.
