# Middleware

Included middleware: correlation id, exception mapping, telemetry, authorization, rate limiting and idempotency. Middleware is ordered using the builder and wraps handlers in reverse registration order.
