# Security

Controls include token redaction, chat id redaction, webhook secret validation, authorization policy, rate limiting, idempotency, payload limit in webhook sample, Markdown/HTML escaping and `.env` exclusion.
