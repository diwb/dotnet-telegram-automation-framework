# Testing

Validated locally with `dotnet test TelegramAutomation.slnx -c Release`: 90 passing xUnit cases. Coverage collection is configured through coverlet collector; threshold enforcement is intended for CI expansion.
