# Troubleshooting

If restore reports SQLitePCLRaw.lib.e_sqlite3 vulnerability, ensure the direct package reference remains at 3.53.3 or newer. If webhook returns 401, verify the `X-Telegram-Bot-Api-Secret-Token` header.
