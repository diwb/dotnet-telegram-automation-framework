# CLI

`telegramtool` supports doctor, fake start/enqueue/list, bot get-me/send-message, webhook validate, update process-fixture, templates render, outbox list and schedules list. Commands avoid printing real tokens.
