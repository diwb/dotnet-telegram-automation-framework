# Threat Model

Threats considered: token leakage, PII logs, webhook spoofing, replay, callback forgery, command abuse, flood/rate abuse, SSRF/path traversal patterns in user input, Markdown/HTML injection, command injection, tenant leakage, unsafe deserialization, large payload DoS, retry storms and secrets in repo.
