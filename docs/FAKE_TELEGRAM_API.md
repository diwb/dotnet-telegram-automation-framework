# Fake Telegram Bot API

The fake API implements `getMe`, `getUpdates`, `setWebhook`, `deleteWebhook`, `sendMessage`, `editMessageText`, `answerCallbackQuery`, plus admin endpoints for enqueueing updates and inspecting messages.
